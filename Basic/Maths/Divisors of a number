class Solution {
public:
    vector<int> divisors(int n) {
       vector <int>nos;

        for(int i=1; i<=n ; ++i)
        {
            if(n%i==0)
            {
                nos.push_back(i);
            }
        }
        return nos;
    }
};
