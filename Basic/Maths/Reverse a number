class Solution {
public:
    int reverseNumber(int n) {
       int ans ;

       while(n!=0)
       {
       int rem= n%10;
       ans = ans *10+ rem;
       n/=10;
       }
       return ans;
    }
};
